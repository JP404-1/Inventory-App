// Supabase configuration for plain static apps (no Vite).
// Set these values in your Supabase project, then rebuild by reloading the page.

export const SUPABASE_URL = window.SUPABASE_URL || 'YOUR_SUPABASE_URL';
export const SUPABASE_ANON_KEY = window.SUPABASE_ANON_KEY || 'YOUR_SUPABASE_ANON_KEY';

export const STORAGE_BUCKET_PHOTOS = 'item-photos';
export const STORAGE_BUCKET_RECEIPTS = 'receipts';

